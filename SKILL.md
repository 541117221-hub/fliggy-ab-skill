---
name: ab-experiment-report
description: 通用AB实验数据分析与报告生成。支持从fliggy_data_mcp查询实验数据、截图OCR识别、文本输入等多种数据源，自动计算转化率/L2O、绝对提升、相对提升，执行双样本比例Z检验，生成交互式HTML实验报告。适用于AB测试、实验效果评估、对照组实验组对比分析等场景。
---

# AB实验数据分析与报告生成

## 触发条件

当用户提到以下任一关键词时触发：AB实验、A/B测试、实验分析、实验报告、实验组/对照组、实验效果、显著性检验、转化率对比、实验数据、AB test。

---

## 执行流程

### 前置检查：fliggy_data_mcp  availability

在启动任何分析前，**必须先检查** `fliggy_data_mcp` MCP server 是否已连接可用：

1. 尝试调用任意一个 fliggy_data_mcp 工具（如 `mcp__fliggy_data_mcp__list_scenes`）做心跳检测
2. 若返回正常（有数据或空数据但HTTP 200），说明已授权可用，继续执行
3. 若返回 **401 OAuth authentication failed** 或 **连接失败/未找到server**，说明未安装或未授权，必须中断分析流程并提示用户：

> **请先安装并授权 fliggy_data_mcp**：
> 1. 打开 https://code.alibaba-inc.com/trip-platform/fliggy-data-skills
> 2. 按文档完成 fliggy_data_mcp 的下载和OAuth授权
> 3. 授权完成后，重新触发本skill继续分析

**严禁跳过此检查直接询问实验参数**——否则后续所有FDP查询都会失败，浪费用户时间。

---

### 第一步：向用户确认实验参数

触发后**必须先询问**用户选择分析模式，然后按模式收集参数：

#### 模式A：AB实验模式（有实验ID）

| 序号 | 必问信息 | 示例 |
|------|----------|------|
| 1 | 实验ID | 808 |
| 2 | 对照组分组ID | 3876, 3870 |
| 3 | 实验组分组ID | 3877, 3871 |
| 4 | 实验目标人群/诸葛人群ID（若有） | 86416（菲住会员人群包）。**无则不填** |
| 5 | 是否需要人群维度拆分 | 88VIP（`is_88_vip`）、诸葛人群等 |
| 6 | 观测指标口径 | 酒店列表页曝光UV → 详情页UV → 下单UV |
| 7 | 查询日期范围 | 2026-05-08 至 2026-05-13 |

#### 模式B：人群包模式（无实验ID，直接对比两个人群包）

| 序号 | 必问信息 | 示例 |
|------|----------|------|
| 1 | 对照组人群包ID | 147778 |
| 2 | 实验组人群包ID | 129656 |
| 3 | 人群包维度字段名 | group_id / crowd_id / user_group_id 等 |
| 4 | 查询日期范围 | 2026-05-30 至 2026-06-01 |
| 5 | 业务空间 | hotel_decision / hotel_general |

> **人群包模式实战经验**：`hotel_decision` 空间中，`group_id` 维度可用且可命中人群包ID；`crowd_id` 和 `user_group_id` 维度不存在。若用户不清楚维度名，优先尝试 `group_id`。

**询问规则（重要）**：
- **严禁从历史会话或记忆中推荐实验ID**。每次触发skill时，即使之前分析过实验808/798等，也**不得以任何形式提及或推荐历史实验ID**。必须让**用户主动提供当前要分析的实验ID或人群包ID**。
- 对照组和实验组的分组ID也必须由用户主动提供，不得假设或沿用历史数据。
- **询问方式示例**：
  - `请选择分析模式：A-AB实验（有实验ID） / B-人群包对比（无实验ID，直接对比两个人群包）`
  - 模式A：`请提供实验ID、对照组分组ID、实验组分组ID、日期范围。实验是否有目标人群（如诸葛人群包）？如有请提供人群ID，无则留空。`
  - 模式B：`请提供对照组人群包ID、实验组人群包ID、日期范围。如知道FDP维度名也请告知（否则优先尝试group_id）`

> **目标人群ID vs 人群维度拆分的区别**：
> - **目标人群ID（第4项）**：实验本身是全量投放，但用户只想看**特定人群**在实验中的转化。此时用`filter`过滤该人群的数据（如`group_id=86416`），分析对象是该人群在实验组vs对照组的表现。**不拆分维度**。
> - **人群维度拆分（第5项）**：实验数据已包含某维度（如`is_88_vip`），用户想**同时看各子群体的对比**。此时用`dimensions`加入该维度，一次性返回88VIP/非88VIP/全量三组数据。
> - 两者可叠加：先filter目标人群，再按88VIP拆分——但需确认FDP支持该组合查询。
>
> **人群拆分支持情况**：`hotel_decision`空间下，`is_88_vip`维度可用，返回Y/N/null三种值。查询时将`is_88_vip`加入`dimensions`数组即可一次性返回拆分数据。若其他人群维度不可用，再降级到ODPS或截图方式。

---

### 第二步：获取实验数据

**数据源优先级**：FDP自动查询 > ODPS SQL（FDP故障时降级） > 用户提供历史报告 > 截图OCR > 文本输入 > CSV/Excel

#### 2.0 查询方式确认（关键！）

在尝试任何查询前，**必须先确认用户的数据源偏好**，而非直接执行FDP查询：

**FDP可用时**（前置检查通过且非code:9999引擎故障）：
- 自动执行FDP查询，无需额外询问

**FDP不可用时**（code:9999或401等故障），**必须询问用户**选择降级方案：

| 选项 | 数据源 | 操作方式 | 适用场景 |
|------|--------|----------|----------|
| A | ODPS SQL | 调用`fliggy-sql-writer`生成SQL → 用户在ODPS执行 → 贴回结果 | FDP引擎故障，用户有ODPS权限 |
| B | 已有报告文件 | 用户提供HTML/PDF/Excel报告路径 → 提取数据重新生成 | 已有历史报告，无需重新跑数 |
| C | 等待恢复 | 记录参数，FDP恢复后自动继续 | 不急，可等FDP恢复 |
| D | 手动输入 | 用户提供文本/截图/CSV数据 | 以上均不可行 |

> **严禁自动降级**：FDP故障时**不得**不经询问直接调用`fliggy-sql-writer`。必须让用户知情并选择。

#### 2.1 FDP查询（首选）

调用 `mcp__fliggy_data_mcp__fdp_data_olap_query`，param为JSON字符串。

**FDP故障判定流程：**

1. 调用`fdp_data_olap_query`返回异常时，先判定故障类型：
   - **授权/连接问题**（401 / OAuth failed / server not found）→ 中断流程，提示用户安装授权 fliggy_data_mcp
   - **引擎层故障**（code:9999 + `get_meta_data_group`正常返回）→ 触发降级方案，进入2.0询问用户
   - **业务错误**（code正常但返回空数据）→ 检查维度/指标/日期是否正确，调整后重试

2. 判定为引擎层故障的标准：
   - `fdp_data_olap_query`返回`code:9999`且msg为`"olap查询失败：null"`
   - 同一连接下`get_meta_data_group`（businessSpace="hotel_decision"）可正常返回完整元数据
   - `query_fdp_log`返回正常业务错误（如"未查询到日志"）而非授权失败

**参数模板（AB实验）：**

```json
{
  "businessSpace": "hotel_decision",
  "moduleName": "实验分析",
  "joinType": "FULL",
  "dateRange": {"start": "2026-05-08", "end": "2026-05-13"},
  "dimensions": ["biz_date", "abtest_group_id", "is_88_vip"],
  "measures": ["htl_listing_pv_fromlisting", "htl_listing_ipv_fromlisting", "reserve_uv_1d_all_zx_cal"],
  "filter": {
    "concat": "AND",
    "children": [
      {"key": "abtest_id", "operator": "=", "value": "808"},
      {"key": "abtest_group_id", "operator": "in", "value": "'3870','3871','3876','3877'"}
    ]
  },
  "limit": {"count": 1000, "start": 0},
  "extParams": {
    "isCalDayAvg": false,
    "sameTermRatioTypes": [],
    "isRemoveBizDateFilter": true,
    "skillVersion": "1.3.0",
    "query": "实验数据查询"
  }
}
```

> **目标人群/诸葛人群ID过滤**：若用户提供了实验目标人群的诸葛人群ID（如86416），需在`filter.children`中追加人群维度过滤条件。常见维度为`group_id`（人群包ID映射），具体以用户业务侧FDP元数据为准：
> ```json
> {"key": "group_id", "operator": "=", "value": "86416"}
> ```
> 追加后`filter.children`共3条：abtest_id + abtest_group_id + group_id（人群ID）。**若用户未提供人群ID，严禁擅自添加该过滤条件**——否则会导致数据为空。

**参数模板（人群包对比模式，无实验ID）：**

当用户没有AB实验ID，只有两个**人群包ID**时，直接用 `group_id` 维度过滤（`hotel_decision` 空间已验证可用）：

```json
{
  "businessSpace": "hotel_decision",
  "moduleName": "人群包对比分析",
  "joinType": "FULL",
  "dateRange": {"start": "2026-05-30", "end": "2026-06-01"},
  "dimensions": ["biz_date", "group_id"],
  "measures": ["passageway_pv_listing", "htl_listing_ipv_fromlisting", "reserve_uv_1d_all_zx_cal"],
  "filter": {
    "concat": "AND",
    "children": [
      {"key": "group_id", "operator": "in", "value": "'对照组人群包ID','实验组人群包ID'"}
    ]
  },
  "limit": {"count": 1000, "start": 0},
  "extParams": {
    "isCalDayAvg": false,
    "sameTermRatioTypes": [],
    "isRemoveBizDateFilter": true,
    "skillVersion": "1.3.0",
    "query": "人群包对比分析"
  }
}
```

> **人群包模式注意事项（实战经验）**：
> 1. `hotel_decision` 空间中，`group_id` 维度可用且能命中人群包ID；`crowd_id` 维度不存在；`user_group_id` 维度不存在。
> 2. 若 `group_id` 返回空，可尝试 `abtest_group_id` 维度（部分人群包ID复用实验分组ID）。
> 3. 若两个维度都返回空，说明该人群包ID不在FDP底表中，需通过ODPS SQL关联人群包表或请用户提供数据。
> 4. 人群包模式下**无需 `abtest_id` 过滤条件**，直接用 `group_id` + `in` 操作符即可。
> 5. 分析逻辑与AB实验完全一致：人群包A作为对照组，人群包B作为实验组，计算L2O/L2D/D2O、Z检验、生成HTML报告。
> 6. 注意检查两组曝光UV量级是否均衡，若差异较大（如>2倍）需在报告中提示样本不均衡风险。

**关键格式要求（已踩坑验证）：**

| 字段 | 要求 | 错误示例 | 正确示例 |
|------|------|----------|----------|
| `abtest_id` | 字符串 | `808`（整数） | `"808"` |
| `in`操作符value | 每个值加单引号 | `"3870,3871"` | `"'3870','3871'"` |
| `extParams` | `skillVersion`和`query`必填 | 省略 | 见模板 |

**可用指标（推荐组合）：**

| 指标code | 含义 | 漏斗位置 | 备注 |
|----------|------|----------|------|
| `htl_listing_pv_fromlisting` | 酒店listing页曝光UV | 漏斗起点 | **首选**，数据完整 |
| `passageway_pv_listing` | 酒店listing页通道引流曝光UV | 漏斗起点 | 备选，部分日期可能缺失 |
| `htl_listing_ipv_fromlisting` | 酒店listing页引导ipvuv | 漏斗中段 | 详情页UV |
| `reserve_uv_1d_all_zx_cal` | B2C线上四端国内酒店日历下单UV | 漏斗终点 | 下单UV |

**三指标联合查询**：将三个指标放入同一个`measures`数组，可一次性返回曝光、IPV、下单UV，无需分次查询再合并。

**数据完整性检查流程**：
1. 查询返回后检查每个指标每天的值，标记null值
2. 若某日期指标为null，调用`mcp__fliggy_data_mcp__query_fdp_log`获取traceId，分析SQL执行日志确认是底表无数据还是查询异常
3. 若确认底表缺失，寻找替代指标（如`htl_listing_pv_fromlisting`替代`passageway_pv_listing`），重新查询验证

#### 2.2 其他数据源

当FDP不可用或用户选择降级时，按以下优先级处理：

**2.2.1 ODPS SQL（FDP引擎故障时首选）**

调用 `fliggy-sql-writer` skill生成ODPS SQL。用户执行后将结果贴回，继续第三步数据分析。

**2.2.2 用户提供历史报告**

用户提供已有HTML/PDF/Excel报告文件路径 → 用`Read`工具读取 → 提取关键数据表格 → 按当前黄色主题重新生成报告。

**2.2.3 截图OCR / 文本输入 / CSV/Excel**

- **截图/图片**：Read工具读取 → 人工识别表格 → 结构化整理
- **文本输入**：请用户按日期分行提供：曝光UV、IPVUV、下单UV
- **CSV/Excel**：Read工具读取 → 解析结构化数据

---

### 第三步：数据分析

#### 3.1 漏斗指标计算

| 指标 | 公式 | 业务含义 |
|------|------|----------|
| L2D | IPVUV / 曝光UV | 列表页引导能力 |
| D2O | 下单UV / IPVUV | 详情页转化能力 |
| L2O | 下单UV / 曝光UV | 整体转化效率 |

对每一天和汇总分别计算：对照组L2D/L2O/D2O、实验组L2D/L2O/D2O。

#### 3.2 效果度量

- **绝对提升** = 实验组转化率 - 对照组转化率（单位：**pt**，百分点）
- **相对提升** = (实验组 - 对照组) / 对照组 × 100%

#### 3.3 统计显著性检验（双样本比例Z检验）

```python
import math

def norm_cdf(x):
    return 0.5 * (1 + math.erf(x / math.sqrt(2)))

def z_test(n_ctrl, x_ctrl, n_exp, x_exp):
    """n: UV, x: 下单UV"""
    p_ctrl = x_ctrl / n_ctrl
    p_exp = x_exp / n_exp
    p_pool = (x_ctrl + x_exp) / (n_ctrl + n_exp)
    se = math.sqrt(p_pool * (1 - p_pool) * (1/n_ctrl + 1/n_exp))
    z = (p_exp - p_ctrl) / se if se > 0 else 0
    p_value = 2 * (1 - norm_cdf(abs(z)))
    return z, p_value, p_exp - p_ctrl
```

| P值范围 | 显著性 |
|---------|--------|
| < 0.01 | 高度显著 |
| < 0.05 | 显著（决策依据） |
| < 0.10 | 边缘显著 |
| ≥ 0.10 | 不显著 |

#### 3.4 功效分析（未达显著时）

```python
# α=0.05, power=0.8
z_a, z_b = 1.96, 0.8416
n_needed = ((z_a + z_b)**2 * 2 * p_avg * (1 - p_avg)) / (delta**2)
```

---

### 第四步：生成HTML报告

#### 4.1 报告章节结构（v8 标准·七章+实验背景）

完整AB实验报告必须包含以下章节，按顺序排列：

| 序号 | 章节 | 内容要点 | 必选/可选 |
|------|------|---------|----------|
| 0 | **实验背景与策略** | 报告头部 meta 区下方双卡片，明确策略机制（如：策略一·非88VIP挑战通道；策略二·88VIP降门槛）。便于读者理解后续人群效应差异 | 必选 |
| 1 | **数据汇总（日均UV+日均比率）** | 各群体对照/实验的曝光UV/IPV/下单UV（**日均**）+ L2D/D2O/L2O（**日均算数平均**）+ 绝对提升（pt） | 必选 |
| 2 | **统计显著性检验（累计样本）** | 双样本比例Z检验（**仍用累计样本**），Z值/P值/显著性标签 + 功效分析（未达显著时） | 必选 |
| 3 | **增量下单UV估算 + 推全外推** | ΔL2O × avg(PV) 主口径 + 三种口径并行展示 + 推全口径A（×N倍）+ 间夜换算（业务指定系数） | 必选 |
| 4 | **每日趋势可视化** | Chart.js：每日L2O对照vs实验折线图、每日绝对提升柱状图、L2D/D2O折线图（含漏斗时） | 必选 |
| 5 | **N日留存（复购/复访）** | UV加权合并 + 配对t检验，包含**业务消费频次解读**（参考陷阱5模板） | 可选（视业务） |
| 6 | **核心发现（insight-box）** | 3-5条结构化洞察块，每块带颜色背景+标题+要点。常见块：策略与人群效果匹配、外推前提说明 | 必选 |
| 7 | **结论与建议** | 5条建议，最后一条必须包含**价值量化**（推全后日均增量UV/间夜，对比基准说明） | 必选 |

#### 4.2 关键展示规则（v8 沉淀）

**(a) 日均 vs 累计的口径区分（必须在表格上方加 notice）**

| 指标类型 | 展示口径 | 计算公式 | 用于何处 |
|---------|---------|---------|---------|
| UV类绝对量（PV/IPV/下单UV） | **日均** | `合计 / N天` 取整 | 第一章数据汇总 |
| 比率（L2D/D2O/L2O） | **日均算数平均（按天平权）** | `Σ(每日比率) / N天` | 第一章数据汇总 |
| 比率显著性检验 | **累计样本** | `Σ(下单UV) / Σ(曝光UV)` | 第二章Z检验（除以天数会破坏Z检验功效） |
| 增量UV估算 | **日均** | `(总增量UV) / N天` | 第三章 |

> 强制规则：**不得用日均比率做Z检验**。第一章日均比率与第二章累计比率数值差异通常 <0.1pt，但口径必须分开。

**(b) 增量下单UV估算（曝光对等假设·避免伪负值）**

```python
# 主推口径（双方平均PV）
incremental_uv = (l2o_exp - l2o_ctrl) * (pv_ctrl + pv_exp) / 2
# 旁证口径
inc_by_exp_pv  = (l2o_exp - l2o_ctrl) * pv_exp
inc_by_ctrl_pv = (l2o_exp - l2o_ctrl) * pv_ctrl
```

> **伪负值陷阱**：当对照组PV略大于实验组PV时（流量分配抖动），原始下单UV差值可能为负（如 -2,255）。**不能直接报负值**——必须用 ΔL2O × 平均PV 归一化PV规模差异，得到真实正向增量。三种口径并行展示便于读者交叉验证。

**(c) 推全外推口径（最终统一为口径A）**

| 项目 | 标准 |
|------|------|
| 流量配比假设 | **实验桶25% + 对照桶25% + 其他大盘50%**（默认；用户可指定） |
| 推全倍数 | **口径A = 4×当前实验桶增量**（即 1/0.25） |
| 对比基准 | **vs 实验上线前**（完全对照基线，非"当前混合状态"） |
| 单一口径原则 | **仅保留口径A**。已废弃口径B（×3，vs当前混合状态） |
| 必备 notice | "推全增量基于流量配比 25%/25%/50% + 实验组L2O整体替换为对照组L2O整体" |

**(d) 间夜换算（酒店业务专属维度）**

| 项目 | 标准 |
|------|------|
| 换算系数 | `NIGHTS_PER_UV = 1.5`（酒店业务历史经验值，业务方可指定） |
| 计算 | `推全日均增量间夜 = 推全日均增量UV × 1.5` |
| 展示位置 | 推全表格新增列 + 独立**绿色** KPI 卡片（kpi-card.green） + 第七章建议第5条价值量化段落 |
| Notice 文案 | "间夜按 1.5 间夜/下单UV 折算（酒店业务历史经验值）" |

> **跨业务通用化**：交通/景门票等其他业务可类比定义"件均×UV"（如机票×1.2 件/UV），但系数必须由业务方指定，**不要默认套用 1.5**。

#### 4.3 KPI 卡片配色规范（v8 标准·飞猪黄色主题）

| 配色类 | 用途 | CSS class | 具体色值 |
|-------|------|----------|---------|
| 黄色渐变（默认） | 整体核心指标（如全量L2O绝对提升） | `.kpi-card` | `linear-gradient(135deg, #FFC900 0%, #FFDB4D 100%)`，文字 `#5C4300` |
| 蓝色 | 推全后增量UV / 主推口径 | `.kpi-card.blue` | 保留蓝色系以区分业务维度 |
| 绿色 | 推全后增量间夜 / 业务价值量化 | `.kpi-card.green` | 保留绿色系以区分业务维度 |
| 金色 | 已废弃（原口径B辅助参考；v8起不再使用） | — | — |

> **视觉统一规则**：所有报告必须去掉业务logo（如飞猪logo），主色调统一为飞猪App黄色 `#FFC900`。历史深蓝（#1a365d）和淘宝橙（#FF5000）主题已全部废弃。

#### 4.4 insight-box 内容规范

每个 insight-box 为带颜色背景的洞察块，结构为「标题 + 2-3条要点」。常见模板：

| insight-box 主题 | 触发条件 | 核心内容 |
|----------------|---------|---------|
| 策略与人群效果匹配 | 实验含人群拆分（如88VIP） | 策略机制 → 数据效应（杠杆比例）→ 业务含义 |
| 推全外推前提 | 第三章必备 | 流量配比假设 + 对比基准 + 推全倍数计算 |
| 留存指标业务解读 | 含N日复购/复访章节 | 消费频次特性 → 不显著的业务合理性（参考陷阱5） |
| 显著性升级路径 | 历经多次天数迭代 | P值随天数变化趋势表 |

#### 4.5 趋势可视化（Chart.js）

- 每日L2O对照vs实验折线图
- 每日绝对提升柱状图（正负双色）
- 漏斗分析时增加：每日L2D和D2O折线图
- 留存章节：每日复购率/复访率对照vs实验折线图（双图并排）

#### 4.6 视觉规范（飞猪黄色主题）

| 元素 | 规范 |
|------|------|
| KPI卡片 | 黄色渐变背景 `linear-gradient(135deg, #FFC900 0%, #FFDB4D 100%)`，绝对提升为主数字（大号），文字深棕 `#5C4300` |
| 显著性标签 | 高度显著红色 `#CF1322`；显著橙色 `#D46B08`；边缘显著黄色 `#FFC900`（文字深棕 `#5C4300`）；不显著灰色 `#999` |
| 表格 | 表头黄色渐变，汇总行浅黄 `#FFFBF0` |
| 数值颜色 | 正值绿色 `#389E0D`，负值红色 `#CF1322` |
| 标题色 | h1/h2 深金黄 `#D49400` |
| 结论框/数据说明框 | 浅黄背景 `#FFFBF0`，黄色左边框 `#FFC900` |
| Notice 框 | 黄色 `#FFFBF0`=口径说明；绿色 `#f0fff4`=结论确认；蓝色=外推前提 |
| 图表配色 | 对照组灰色 `#8C8C8C`，实验组黄色 `#FFC900`；柱状图正提升黄色 `rgba(255,201,0,0.7)`，负提升红色 `rgba(207,19,34,0.7)` |
| Logo | **严禁**在报告中添加任何业务logo图片 |

#### 4.7 口径标注（必须）

报告中必须注明：下单UV是否为多天去重、曝光UV指标code、观测周期天数、推全外推流量配比、间夜换算系数（含酒店业务时）。

#### 4.8 报告结论必须与数据一致（关键！）

**严禁硬编码结论文字**。所有结论（显著性判断、效应量描述、推全估算、间夜数字、建议）必须通过Python变量动态插入。数据更新后重新生成报告时，若发现结论文字与数据矛盾，必须同步更新。典型错误：
- 数据已显著但文字仍写"未达显著"
- 推全口径切换后第七章建议未同步
- 间夜系数变化后KPI卡片与建议段不一致

---

### 第五步：输出保存

1. 默认保存路径：`用户工作目录/outputs/实验{ID}-{名称}/`
2. 文件名：`{实验名称}实验效果分析报告.html`
3. 使用 `present_files` 展示给用户

---

## 常见陷阱与注意事项

### 陷阱0：人群包模式维度探测陷阱（新增）

人群包模式下，**不要假设维度名为 `crowd_id` 或 `user_group_id`**。

**踩坑实录**：
- 用户说人群包ID是146449/146450，直接尝试 `crowd_id` → 报错"column does not exist"
- 再尝试 `user_group_id` → 同样报错
- 最终用 `group_id` → 查询成功

**探测规则**：
1. 优先尝试 `group_id`
2. 若返回空，尝试 `abtest_group_id`
3. 若都失败，直接询问用户"这个人群包ID在你们那边对应FDP哪个字段？"
4. 严禁反复尝试不存在的维度（浪费API调用且用户体验差）

---

### 陷阱1：Python f-string生成HTML时字符串截断

当使用`html = f'''...'''`包裹HTML时，若HTML中包含JavaScript数据注入如`data: ['''`或`''']`，三重引号会**提前结束**Python字符串，导致后半段变成普通字符串，`{{`无法被f-string转义为`{`，最终HTML中Chart.js出现`{{`语法错误，图表无法渲染。

**修复方案**：
- 方案A：改用`f"""..."""`包裹（JavaScript用单引号字符串）
- 方案B：将Chart.js数据部分拆分为独立变量，用`{chart_js}`插值
- 方案C：生成后用`sed`修复：`sed -i 's/{{/{/g; s/}}/}/g'`

### 陷阱2：数据更新后结论未同步更新

用户追加新数据后，显著性可能从"不显著"变为"显著"。报告中的文字结论、建议（如"继续观察"改为"可决策推全"）必须同步更新。**结论部分严禁写死**，应通过变量动态生成。

### 陷阱3：T-1日数据延迟误判为底表缺失

FDP底表数据通常T-1可用，但实际可能**延迟1-2天**。当某天所有指标为null时，不要立即判定为"底表缺失"。应先调用`query_fdp_log`排查trace：若SQL正确执行但返回空，**隔天重查**大概率能补齐。

**判断流程**：
1. 发现某天指标为null
2. 调用`query_fdp_log`获取traceId，检查生成的SQL是否正确
3. SQL正确但返回空 → 记录为"可能延迟"，隔天重查
4. 隔天重查仍为null → 确认底表缺失，寻找替代指标

### 陷阱4：小效应量实验天数不足导致误判

效应量<0.2pt的实验在6天内大概率不显著（P>0.10），易被误判为"无效"。随着天数增加，样本量累积后显著性会持续提升。

**经验参考**（实验808快升F4）：

| 天数 | 全量L2O | 全量P值 | 非88VIP P值 | 88VIP P值 |
|------|---------|---------|-------------|-----------|
| 6天 | +0.06pt | 0.4500 | 0.8800 | 0.3400 |
| 9天 | +0.15pt | 0.0106 | 0.1160 | 0.0500 |
| 10天 | +0.17pt | 0.0027 | 0.0556 | 0.0195 |
| 11天 | +0.18pt | 0.0009 | 0.0440 | 0.0082 |

> **规则**：效应量<0.2pt时，建议至少跑7-10天再下结论。若6天不显著，不要轻易否定实验方向。

---

### 陷阱5：留存类指标不显著时缺少业务解读

7日复购率/复访率、N日留存等"长周期"指标若实验组与对照组无显著差异，**不能仅报P值结论**，应结合业务**消费频次特性**给出业务解读，避免被误判为"实验无效"。

**判断框架：**

| 业务类型 | 消费频次 | 7日复购预期变化 | 解读模板 |
|---------|---------|----------------|---------|
| 低频（酒店/机票/旅游度假） | 数月一次 | 几乎不会因前端体验改善而提升 | "复购主要由出行需求驱动，前端UI优化不会凭空创造新需求" |
| 中频（外卖/打车） | 周级 | 体验改善对短期复购有部分迁移 | "若复访率有微弱正向趋势，可作为长期累积效应的早期信号" |
| 高频（电商/内容/资讯） | 日级 | 体验改善对复购影响明显 | "若复购不显著，需检查实验是否真正改善了核心体验" |

**报告写作规则：**
- 留存指标不显著时，**必须同时给出业务解释段落**，说明"为什么不显著属预期"或"为什么需要警觉"。
- 不要把留存不显著作为推全的阻断项（除非业务方明确提出）；推全决策应以即时转化指标（L2O）为主，留存指标作为长期监控盘。
- 若留存指标方向上微正向（即使P值不显著），应单独说明"方向上与业务假设一致，但效应量过小，建议长期监控"。

---

### 陷阱6：增量UV估算的"伪负值"陷阱

当对照组PV略大于实验组PV时（流量分配抖动 ±2%），直接用「实验组下单UV - 对照组下单UV」会得到**负值**（如 -2,255 单）——会被误判为实验"反向"。但实际上 ΔL2O 仍为正向，PV规模差异掩盖了真实增量。

**核心规则：增量UV估算必须做PV规模归一化。**

```python
# ❌ 错误：直接看下单UV原始差值
incorrect_inc = order_uv_exp - order_uv_ctrl   # 可能负值

# ✅ 正确：用 ΔL2O × 平均PV 归一化
delta_l2o = (order_uv_exp / pv_exp) - (order_uv_ctrl / pv_ctrl)
correct_inc = delta_l2o * (pv_ctrl + pv_exp) / 2
```

**展示要求**：报告必须并行展示三种口径（实验组PV / 对照组PV / 双方平均PV - 推荐），并在 notice 中说明"避免曝光规模差异导致的伪负值"。

---

### 陷阱7：推全外推口径混乱（口径A vs 口径B）

历史上曾并行展示两种推全口径，导致读者混淆：
- **口径A**（×4倍 = 1/0.25）：vs **实验上线前**（完全对照基线）。**唯一推荐口径**。
- ❌ 口径B（×3倍）：vs 当前混合状态（已包含部分实验流量）。**v8起已废弃**。

**统一规则**：
- 报告中**只展示口径A**，不再展示口径B
- 推全 KPI 卡片只用**蓝色**（不再用金色辅助卡）
- 不在卡片上加"主推""参考"等标签——单一口径无需对比
- notice 只写"vs 实验上线前"对比基准，不再说明双口径差异

---

### 陷阱8：报告章节结构在迭代中漂移

随着用户多轮补充需求（增加间夜、增加策略说明、调整口径），章节顺序容易混乱。**v8起固定七章+实验背景结构**（详见 4.1），新增内容应归入既有章节，而非新建章节。

| 新需求类型 | 应归入章节 |
|-----------|----------|
| 业务价值换算（间夜/件均/客单价） | 第3章「增量UV+推全外推」+ 第7章建议第5条 |
| 策略机制说明 | 第0章「实验背景与策略」 |
| 人群杠杆效应分析 | 第6章 insight-box「策略与人群效果匹配」 |
| 长周期留存补充 | 第5章「N日留存」 |

---

## 多维度实验处理

当实验含交叉维度（如88VIP × 实验/对照）：

1. **FDP人群维度支持**：`hotel_decision`空间下，在`dimensions`中加入`is_88_vip`即可一次性返回按88VIP拆分的每日数据，返回值为Y/N/null三种。需在分析时过滤null行。
2. 各维度子群体独立分析 + 全量汇总（样本加权）
3. 对比子群体效果差异，在结论中分析原因

---

## 术语与精度规范

| 项目 | 规范 |
|------|------|
| 单位 | 绝对提升用 **pt**（百分点），不用pp |
| 转化率术语 | 统一用 **L2O**（List to Order），不用CVR |
| 增量术语 | "增量下单UV" / "增量间夜"（业务侧）；"绝对提升" / "ΔL2O"（统计侧） |
| 推全术语 | "推全后日均增量"（不用"年化")，对比基准必须明示"vs 实验上线前" |
| 间夜换算 | 1.5 间夜/下单UV（酒店；业务可调）。展示时必须带 notice 说明系数来源 |
| 流量配比 | 默认假设 25%/25%/50%（实验/对照/其他）；用户指定其他比例时按用户参数计算 |
| 日均 vs 累计 | UV/比率展示用日均，显著性检验用累计，必须在 notice 中分开标注 |
| P值 | 保留4位小数 |
| Z值 | 保留3位小数 |
| 绝对提升 | 日报保留2位小数，汇总保留4位小数 |
| 日报行数 | >10天时可折叠，默认展示汇总+图表 |
| 数字千分位 | 推全/间夜等大数字必须用 `{:,.0f}` 格式（如 1,440） |

---

## 任务结束前写回检查

每次完成AB实验报告任务后，必须执行以下检查清单，将本次踩坑经验固化为Skill规则：

### 检查清单

1. **本次是否出现 Skill 没覆盖的错误？**
   - 若出现FDP查询新错误、参数格式异常、数据返回结构变化等，更新`第二步：获取实验数据`章节

2. **是否有新的项目约定需要写入？**
   - 若用户提出新的术语偏好、报告格式要求、数据口径定义，更新`术语与精度规范`

3. **是否有新的测试命令、验证命令需要记录？**
   - 若发现新的数据验证方法（如query_fdp_log的新用法），更新`数据完整性检查流程`

4. **是否有一个失败样例可以加入评测？**
   - 若本次出现典型错误（如f-string截断、结论与数据矛盾、天数不足误判），写入`evals/ab-experiment-report/cases.md`

5. **是否有一句规则可以减少下次沟通？**
   - 若用户反复提出同一类要求（如"突出绝对提升"），将其固化为Skill默认行为，避免每次都问

### 写回规则

- **Skill规则** → 修改`~/.qoderwork/skills/ab-experiment-report/SKILL.md`
- **项目规则** → 写入`AGENTS.md`（如适用）
- **可回归样例** → 写入`evals/ab-experiment-report/cases.md`
- **一次性任务细节** → 仅写入当日memory，**不**写入通用Skill
